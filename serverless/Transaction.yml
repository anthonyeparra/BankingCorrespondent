Transaction:
  handler: Handlers/TransactionHandler.transaction
  timeout: ${self:custom.timeout}
  events:
    - http:
        path: /transaction
        method: POST
        cors: ${self:custom.cors}
